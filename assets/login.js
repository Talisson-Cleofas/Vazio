const form=document.getElementById("loginForm"),user=document.getElementById("username"),pass=document.getElementById("password"),err=document.getElementById("errorBox"),btn=document.getElementById("submitBtn"),toggle=document.getElementById("togglePassword");toggle.addEventListener("click",()=>{const show=pass.type==="password";pass.type=show?"text":"password";toggle.textContent=show?"Ocultar":"Mostrar"});form.addEventListener("submit",async e=>{e.preventDefault();err.textContent="";btn.disabled=true;btn.textContent="Verificando…";try{const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},credentials:"same-origin",body:JSON.stringify({username:user.value.trim(),password:pass.value})});if(r.ok){location.replace("/app");return}if(r.status===429){err.textContent="Muitas tentativas. Aguarde alguns minutos e tente novamente."}else{err.textContent="Credenciais inválidas."}pass.value="";pass.focus()}catch{err.textContent="Não foi possível autenticar agora. Tente novamente."}finally{btn.disabled=false;btn.textContent="Entrar com segurança"}});